// Key-typed Object.keys / Object.entries. The implementation lives in
// @chaos-master/core (byte-identical to the copy this file used to hold); the
// app keeps importing it from here.
export { recordEntries, recordKeys } from '@chaos-master/core'
